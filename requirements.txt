# Tapetide Risk Radar — ADK multi-agent app (deployed to Cloud Run).
google-adk>=1.0.0
google-cloud-firestore>=2.19.0
google-cloud-bigquery>=3.25.0
google-genai>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.9.0
python-dotenv>=1.0.1

# Local signal-SQL validation prototype (risk_radar/) also needs:
#   psycopg2-binary>=2.9.9  (not required by the deployed agent)
